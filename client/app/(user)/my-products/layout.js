const MyProductsLayout = ({ children }) => {
    return (
        <div className="w-full h-full p-0 m-0 flex justify-center items-center">
            {children}
        </div>
    );
};

export default MyProductsLayout;
